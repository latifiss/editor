import type { Config } from "tailwindcss";

const config: Config = {
  // Tailwind v4 uses CSS-based configuration
  // This file exists to satisfy the PostCSS plugin import requirement
};

export default config;

